# React_WeatherApp
React Application to access weather application with API 'https://api.openweathermap.org/data/2.5/weather'

to use project:
1. create .env to root directory
2. add your API_KEY to .env wit format API_KEY = 'your API Key'
3. save your .env file
4. Run your project with npm run start

